nima:
  server:
    port: 8080
    sockets:
      - name: "https"
        port: 8081
        tls:
          private-key:
            keystore:
              resource.resource-path: "certificate.p12"
              passphrase: "helidon"