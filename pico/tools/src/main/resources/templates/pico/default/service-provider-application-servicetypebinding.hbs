/**
         * In module name "{{modulename}}".
         * @see {@link {{servicetypename}} }
         */
        binder.bindTo({{activator}})
                {{#injectionplan}}{{{.}}}
                {{/injectionplan}}.commit();
